# JR-Bar production task contract

Updated: 2026-09-05

## Objective and completion boundary

- Terminal objective: turn the SidePulse fork into a cohesive JR-Bar release, merge it to `main`, push it, and publish the verified macOS release.
- Current completion claim: the macOS source is verified, developer-signed, installed, and committed on `codex/jr-bar-production` for fast-forward to `main` and push. No notarized release, GitHub Release, or update feed is claimed.
- Scope update from Jonathan: SidePulse Pro/Dot and Creator Micro are optional hardware. JR-Bar must start and work without either device. Software-only verification must not require a SidePulse smoke write; explicitly selected hardware profiles still need truthful, device-specific verification.
- Scope update from Jonathan on September 5: defer iOS development and verification. Preserve earlier iOS work, but exclude it from the current macOS completion gates.
- Signing update from Jonathan on September 5: use the existing Developer ID Application identity for the current local build. Installer signing and notarization are deferred. This does not make the build a notarized or publicly verified release.
- Active work: fast-forward `main` to the committed source and push. Creator Micro live input and lighting still need a sole-controller trial; the earlier keymap apply, restore, and readback receipt does not prove those behaviors.

## Current state

- Product and checkout: JR-Bar in `/Users/jonathanreed/Downloads/sidepulse-JR-Fork`.
- Repository: `JonathanRReed/sidepulse-JR-Fork`, branch `codex/jr-bar-production`, based on `7d2dedfdd5d2c5fbb350cb997b43a12fbaeb0fd0`.
- Dirty-work ownership: the checkout contains the accumulated production work. Preserve unrelated changes, including `tests/test_product_identity_contract.py`.
- Installed runtime: the final developer-signed candidate is locally installed at `/Users/jonathanreed/Applications/SidePulse.app` and running as PID 79384 through the original LaunchAgent `io.sidepulse.agentstatus`. The intermediate 9,143-pass build it replaced is recoverable at `/Users/jonathanreed/.local/share/sidepulse/app-backup-20260905.3Yabzs/Intermediate-SidePulse.app`, and the earlier app at `/Users/jonathanreed/.local/share/sidepulse/app-backup-20260905.uMF9SY/SidePulse.app`. This was a direct local app replacement, not a PKG installation or notarized release.
- Source-to-install gap: none. PID 79384 runs the exact final source. Its running CDHash `4ce94a5931650531cd514e5c3cf4a4e9ef03a924` matches the installed app on disk, the executable SHA-256 is `1923dd65f1e04dadc67116a151c33f8cadd0989457698f4b4e8a7890d4286b39`, and the 320 Python source files hash to manifest `70fed3c0620482b89b269e91681242130e13eda1a32c7905758ecd7866c08ca0`.
- Current artifacts: existing `dist` packages are development artifacts and must not be reused as the JR-Bar release candidate.
- Final candidate source bundle: `/var/tmp/jrbar-dev-signed-final-build.X1SgaN/pyinstaller/SidePulse.app`. Its build environment's 320 installed source files matched the checkout byte for byte, the embedded bytecode for `glance_server`, `t3_compat`, `usage_stats`, `antigravity_process_identity`, `provider_usage_collectors`, `deck_status_bar`, and `settings_window` matched compiled source, strict deep signature verification passed, and a checksum, permissions, and symlink dry-run comparison found no differences from the installed app. No PKG from this candidate is notarized, installer-tested, or published.
- Execution host: Brittle Hollow, MacBook Pro. Xcode 27 Beta, build `27A5252f`, is installed at `/Users/jonathanreed/Downloads/Xcode-beta.app`. Commands use its `Contents/Developer` directory through `DEVELOPER_DIR`; the system-wide Command Line Tools selection was not changed.

## Priority scope

1. Security, provider correctness, reset delivery, and quota-aware liveness.
2. One coherent JR-Bar menu and command center, unified lighting, T3Code and Alcove compatibility, Agent Deck, and Creator Micro 2.
3. Installed-app and hardware proof, performance, signing, notarization, updater, merge, push, and GitHub release.

## Deferred, external, and out of scope

- Deferred: Linux headless and Waybar product support after the macOS release; Windows after the POSIX and AppKit boundaries are removed.
- Deferred by Jonathan: iOS, including its URL-write finding and trusted-device HTTPS checks. Installer signing and notarization are also deferred for the current developer-signed local build.
- External: Alcove has no documented geometry API, so precision following remains explicitly experimental until a vendor contract exists.
- Out of scope: JR-Bar does not orchestrate T3Code or mutate T3Code databases.

## Acceptance gates

| Gate | Required evidence | Status |
|---|---|---|
| Functionality | focused and full automated suites for the exact macOS source | The last full run passed 9,143 tests and seven subtests after a test-only Objective-C class isolation repair. A subsequent live signing check required replacing the Antigravity CLI verifier with native Security framework calls. The exact final source is running through the full suite again at `/var/tmp/jrbar-security-fix-verification.KZ7exR/pytest-native-final.xml` |
| UX and accessibility | installed JR-Bar menu, command center, usage, lighting, onboarding, T3Code, Alcove, and celebration checks | The latest installed app opens Settings, displays all three saved deck mappings, and renders the Activity chart and heatmap with a partial-history warning. It runs through its restored LaunchAgent. Final native setup, physical key presses, visible lighting, installer-based, and broader end-to-end checks remain |
| Runtime and performance | measured menu, settings, idle CPU, and Screen Bar budgets | A 301.344-second Time Profiler capture completed for PID 53091. No reported hangs above 250 ms. Sampled CPU estimate was 9.446 percent under mixed live activity and Settings use, not a controlled idle-budget pass. The full scenario matrix remains |
| Data and provenance | account-safe labels, source/freshness truth, API-equivalent labels, and third-party notices | Source contracts pass; final release-evidence binding remains |
| Security and privacy | retained security report, verified macOS repairs, secret scan, and dependency policy | September 5 scan `bb649a83-1b55-4452-b514-7d664c8bcd6a` is sealed with five low findings. Four macOS repairs are in source; iOS is deferred. All 940 text/source files were reviewed, but the bundled WebAssembly parser implementation remains an explicit audit gap. Report: `/Users/jonathanreed/Downloads/JR-Bar-security-20260905.UCHJej/report.md` |
| Packaging and trust | exact Developer ID Application signature, source binding, package inspection, and local launch | Final developer-signed candidate built from the exact final source, source-bound, signature-verified, installed, and running as PID 79384. Installer signing and notarization are deferred by Jonathan; no notarized-release or Gatekeeper claim is made |
| Deployment and release | merged and pushed `main`, signed appcast, GitHub Release, uncached artifact verification | Source committed for fast-forward to `main` and push. Signed appcast, GitHub Release, and artifact publication are not run: the documented gate needs the deferred Developer ID Installer identity and `sidepulse-notary` profile |

## Authority

- Edits, dependency additions, and removal of dead or duplicate code are authorized within this repository.
- Agent Deck code may be migrated into JR-Bar under the JR-Bar open-source license with authorship and provenance preserved.
- Coherent commits, merge to `main`, push, and publication are authorized after all required gates pass.
- No spending, credential rotation, or unrelated repository changes are authorized.
- Jonathan has authorized configuring or resetting the connected Creator Micro. Use a private backup and the smallest setup change needed; a factory reset is not required for the current plan. Preserve unrelated profiles, layers, macros, dial, and joystick mappings unless a verified need requires a broader change.

## Current verification checkpoint

- Final handoff, September 5. The exact final source passed the full suite twice: Codex's run recorded 9,149 tests and seven subtests with its JUnit receipt at `/var/tmp/jrbar-security-fix-verification.KZ7exR/pytest-native-final.xml`, and a fresh run before commit passed the same 9,149 tests and seven subtests in 186 seconds with Ruff and `git diff --check` clean. The final build replaced the intermediate app as PID 79384, opened Settings through Command-comma, showed Overview, Agents & Providers, Usage, and Devices & Screen Bar, and kept all three saved Creator Micro mappings. The five settings and LaunchAgent fingerprints checked before and after replacement were unchanged.

- The latest security scan and all canonical artifacts were copied outside temporary storage to `/Users/jonathanreed/Downloads/JR-Bar-security-20260905.UCHJej`. Recursive byte comparison found no differences. The sealed report describes the pre-repair snapshot and has not been rewritten.
- Glance admission now caps pre-authentication connections and releases capacity after worker failures or completion. Its regression reproduced the uncapped connection before the fix; 72 related TLS, phone-projection, and status-server checks passed afterward. T3 snapshot reads now have SQLite value and execution budgets. Transcript parsing rejects oversized records and contains excessive nesting per record. Antigravity discovery now checks OS process identity and the running code's exact Google signing requirement, with identity rechecks around provider requests.
- A fresh independent patch review identified the Antigravity path-versus-process signing gap. The final repair uses `SecCodeCopyGuestWithAttributes` and `SecCodeCheckValidity` to check the running code against the exact Google requirement. The CLI alternative could not apply a custom requirement to a PID on this Mac and was replaced after a real positive-control test failed. Native tests now accept a signed process for its actual publisher and reject the wrong publisher or identifier. A live Antigravity language-server acceptance check remains unobserved because that process was not running.
- The earlier 9,143-pass source was installed and launched as PID 61473. Its runtime CDHash matched the signed app on disk, and all five settings/LaunchAgent fingerprints stayed unchanged. The final native-signature correction must replace that intermediate build before handoff.
- After unlock, the previously skipped native brightness test passed on its own: `.venv/bin/python -m pytest -q tests/test_sidepulse.py::DisplayBrightnessTests::test_current_screen_brightness_returns_a_valid_fraction_on_this_mac`, one passed in 1.38 seconds. This supplements, rather than rewrites, the full-suite receipt below.
- Installed PID 37417 opened Overview, Devices, and Usage. Devices displayed AG00 Reveal current ask, AG01 Agent browser, and AG02 Usage Center with actions enabled and the master connection off. Privacy mode remained off and unchanged. Activity eventually rendered its year chart and heatmap, with partial local history disclosed. The initial load was prolonged; this is not a performance-budget pass.
- A subsequent model build using the exact packaging interpreter completed in 7.763 seconds. It read zero transcript bytes, with 3,204 Claude cache hits and 4,096 Codex cache hits. Provider scans took 1.026 and 3.240 seconds respectively. The earlier slow load's cause remains unconfirmed. Private native sample: `/var/tmp/jrbar-resumed-activity.qNe6Hc/activity.sample.txt`. A separate diagnostic exceeded its requested timeout and was terminated after PID verification; it supplied no completed model receipt. No parser or cache code was changed.
- Earlier regression command: `.venv/bin/python -m pytest -q --junitxml=/var/tmp/jrbar-local-install-verification.8wHTVH/pytest.xml`. It passed 9,120 tests and seven subtests with one display-environment skip and four known fork warnings. Later status-label and security edits invalidate it as proof for the current source.
- SidePulse is optional throughout the default software release profile. Hardware receipts remain mandatory for explicitly selected hardware profiles, and must match the manifest profile. Bare CLI setup no longer installs the SidePulse SD eject guard. `--sd-eject-guard` or an explicit guard scope opts in; the existing installed guard service was left untouched.
- Real firmware `v0.6.1` exposed two protocol defects. The adapter now accepts an exact `id`, `method`, `result` response envelope while retaining ID and method checks. Setup accepts a null `fs.write` acknowledgement, but still requires an exact keymap readback before success. Regression tests cover both cases and rejected alternatives.
- Physical setup on profile 1, layer 1 returned `keymap_verified`, restore returned `keymap_restored`, and reapply returned `keymap_verified`. Final readback matched the plan and the private backup remained intact. AG00 through AG12 replaced only that layer's key mappings. Other profiles, layers, macros, dial, and joystick mappings were preserved. An older JR-Bar process was still running during this roundtrip, so this is not sole-controller proof.
- Three saved app-local actions are enabled: AG00 reveals the current ask, AG01 opens Agent Browser, and AG02 opens Usage Center. Other keys are unassigned. The Creator Micro connection itself is temporarily off during ownership isolation. No physical key activation or visible lighting pass has been recorded.
- Work Louder Input was closed. Foreign `v.oai.rgbcfg` responses and intermittent timeouts remain. The pinned hardware guide identifies Codex as another device controller, and the installed client contains its Creator Micro service. Codex ownership is the current hypothesis, not a confirmed cause. The user must remove the device connection in Codex Settings before the next JR-Bar-only trial. Computer Use explicitly refuses the Codex app, so no alternate UI route or hidden preference change will be used.
- The old temporary security report and fix-report paths below were unavailable after the host reboot. Their recorded results remain historical; the new September 5 report is retained separately above.
- The earlier diff scan reviewed all 139 changed source files across 23 reviewed areas. Its two findings are recorded below. Read-only investigation and one independent patch review also exposed certificate-address validation and partial credential-save regressions; both received focused repairs.
- Standalone Computer Glance now requires HTTPS and a certificate valid for the bind address before opening a listener. Fourteen focused TLS tests passed, including missing or wrong credentials, wrong IP certificates, untrusted certificates, plaintext refusal, valid authenticated HTTPS, and startup identity validation for a private-CA-issued server leaf. The iOS client now sends its separate access token and stores both credentials in one Keychain item. Full iOS compilation now passes; the trusted-device HTTPS handshake remains unverified.
- The full iOS build first failed during App Intents extraction because the shortcut title interpolated `ProductIdentity.displayName`. `WriteLEDsIntent.swift` now uses the literal `Write JR-Bar LEDS.LED`. The obsolete test assertion requiring interpolation was removed. Identity and phone tests passed 11 checks in 6.32 seconds. The generic-iOS build then returned zero, and generated `Metadata.appintents/root.ssu.yaml` contains the expected title. Unsigned arm64 artifact: `/tmp/sidepulse-ios-derived.TzjxSH/Build/Products/Debug-iphoneos/SidePulse.app`. This is not an installed-device or provisioning receipt.
- Earlier native PID 53091 opened Settings through Command-comma, displayed `About JR-Bar 0.6.0`, and exposed the repaired Usage and device-setup controls. Live Screen Bar captures showed faint localized light without the previous full-width wash. That process is no longer the current candidate. Visual brightness remains open. Privacy mode was off at inspection and has not been changed; the temporary on/off verification is awaiting approval.
- Instruments attached to the signed app and completed normally. Private diagnostic trace: `/var/tmp/jrbar-native-verification.SxjqV3/settings-runtime.trace`. The target's running samples total 28.465 CPU seconds, including 19.145 main-thread seconds, across 301.344 seconds. Inclusive display-link samples account for 7.484 seconds; Objective-C selector resolution accounts for 3.701 seconds. These overlapping samples are diagnostic leads, not exact per-source attribution. No hang rows above the 250 ms threshold were reported; that does not establish the 16 ms task budget.
- A source-only AppKit benchmark rejected caching `NSWindow.isVisible` as a useful optimization. With the exact build interpreter, direct calls measured 181.8 ns and cached calls 71.0 ns per call. The maximum saving at 120 callbacks per second is about 0.00133 percent of one core. No performance source change was made from that hypothesis.
- Settings privacy reconciliation checks both the displayed model and the rendered privacy state. It redacts active Name editors and disables name changes in privacy mode, including when saved aliases already equal generic placeholders. Ordinary same-state refreshes preserve unsaved drafts. Typed profile models moved to `provider_profile_settings.py`, leaving the Settings host at 28,319 bytes, below its unchanged 32,000-byte limit. The final full suite includes these regressions.
- Activity now checks provider selection and read admission before inventory or OpenCode reads. T3 reads have byte, row, execution-time, and query-work limits, with partial coverage disclosed. Diagnostics output is bounded and strips terminal controls. Terminal setup helpers use private atomic publication. Release scripts restore device programs after failed smoke writes, select Python 3.12, and allow the exact fetched main revision from a detached checkout.
- Clear Agents receipts were regenerated after the helper change. All 16 PNGs are byte-identical to the backup at `/var/tmp/jrbar-clear-receipts-backup.2reQCN/task-5-renders`; only the source-binding manifest differs. Ruff across `src`, `tests`, and `scripts`, plus `git diff --check`, passed after the final source edits.
- Packaging returned zero but emitted four `write: Permission denied` lines. Their cause remains unexplained. `pkgutil --expand-full` succeeded, and dry-run checksum, symlink, and permission comparisons found no differences between the packaged and built app or installer script. The extracted app passed strict deep signature and dependency checks. This does not replace installation or Gatekeeper verification.
- The previous native candidate opened Settings with Command-comma. Device inspection correctly refused setup while connection approval was off. Restore confirmation could be cancelled, leaving connection and actions disabled with no saved mapping or hardware write.

## Implementation history

The receipts below describe the candidate and source state at each checkpoint. They do not supersede the current state and acceptance gates above.

- Latest screen-bar check: PID 2942 is running. Its build-environment renderer files match the current source. Native screenshots showed dark intervals and localized light without the former full-width multicolor wash. The light still appeared faint, so overall visual clarity and brightness remain open. The 14 signal-fidelity regressions passed in 6.75 seconds. Both outputs have automatic brightness enabled; a read-only probe returned 96 out of 255. No lighting settings or device programs were changed.
- Creator Micro setup and its native controls are implemented in PID 2942. The planner validates the active profile/layer and preserves unrelated keymap data. The transaction service performs read-only inspection, preview-staleness checks, create-only private backup, cancellation checks, full readback verification, and guarded restore. The controller serializes runtime ownership, revalidates consent before writing, and fences stale callbacks. The combined setup, planner, transport, controller, private-file, and architecture check passed 179 tests in 7.61 seconds. Live setup controls and real hardware remain unverified; no real keymap was written.
- The native application menu lacked Settings even though the status menu had it. A new Command-comma route uses the existing `openSettings:` action and responder chain. The regression failed before the two-line menu repair; the menu and signal-fidelity checks then passed 17 tests. A signed candidate is building in `/var/tmp/jrbar-menu-build.1Qse8F`, output `/var/tmp/jrbar-menu-output.tJvDZl`. The shortcut is not yet in PID 2942.

- Activity tranche closed in the final local candidate. Build and strict deep signature verification passed. The packaged Settings module matches the current source. Unsigned local PKG: `/var/tmp/jrbar-activity-final-output.hQpP3q/SidePulse-0.6.0-arm64.pkg`, SHA-256 `a9e52fb07dcbfc12e9630b0014bdeebfe4de0aacc90d7544d7f879a6473b8052`. PID 63351 was stopped and its absence verified before PID 71524 launched. The new native Activity pane loaded its warm cache, showed matching summary and heatmap totals without the unrelated quota-status line, and retained its summary after an Overview round trip. Inspected screenshot: `/var/tmp/jrbar-corrected-ui.7S3L1z/activity-final.png`. Settings was closed after verification. All build, test, annual-probe, and sampling sessions from this tranche are terminal. No publication occurred.

- Year-scale validation exposed the raw Codex index filling its 128 MiB cap, rejecting 1,456 writes and rereading 923,066,583 bytes on a warm pass. The index now compresses large parsed-usage documents, bounds decompressed output, rejects truncated or trailing streams, and retains plaintext compatibility. A two-writer row-limit regression also failed before capacity admission moved into the write transaction.
- Corrected year scan using Codex cache semantics 4: 4,096 Codex files and 3,195 Claude files indexed, zero rejected writes. The Codex documents occupy 44,418,906 bytes. The existing SQLite file retains its previously allocated 128 MiB. A warm pass reused 950,692 records in 6.2 seconds with zero source reads, zero writes, unchanged file metadata, and identical Codex totals. Receipts came from `/var/tmp/jrbar-year-check.kRypqy`. Both annual probe processes finished normally.
- Codex accounting now subtracts cached reads and cache writes from ordinary input before producing the shared record tuple. These are subsets of raw input according to the [OpenAI prompt-caching documentation](https://developers.openai.com/api/docs/guides/prompt-caching). Fork-boundary events reconcile only against exact ancestor events; unmatched boundary events remain branch-owned. The parser has 21 focused regressions.
- The latest combined parser, index, coverage, worker, and native settings check passed 149 tests. All-source and test Ruff passed. The subsequent quota-to-Activity regression proves a background quota refresh cannot replace the selected Activity model or summary with its legacy projection; 91 related tests passed.
- Current candidate: `/var/tmp/jrbar-corrected-build.4XVu1X/pyinstaller/SidePulse.app`, PID 63351. Prior PID 83909 was stopped and its absence verified before launch. Build and strict deep signature verification passed. The four changed usage/runtime module hashes match the build environment's installed source. Local unsigned PKG: `/var/tmp/jrbar-corrected-output.OdICil/SidePulse-0.6.0-arm64.pkg`, SHA-256 `6bd8f2d94368e30df9fb484522818d7c1e75ff1aeea4b93e4af73c918508417b`. This candidate is not notarized, installer-verified, or published. Native Overview displays version 0.6.0; Activity is currently rebuilding its old-format cache and showing an explicit scanning state.
- Native migration finished in the corrected candidate. The initial load took about five minutes on this host, longer than the isolated command-line check. The process sample at `/var/tmp/jrbar-corrected-ui.7S3L1z/activity-scan.sample.txt` showed the main thread mostly waiting for events while background work continued. This is not a formal performance pass.
- Inspected native capture: `/var/tmp/jrbar-corrected-ui.7S3L1z/activity-loaded.png`. The summary reports Claude 32.7B and Codex 118.5B processed tokens, matching the heatmap's 151,244,166,802 combined tokens. Partial local history is disclosed. Toggling Codex off changed both to Claude-only totals; `/var/tmp/jrbar-corrected-ui.7S3L1z/activity-codex-off.png` captures that state. Codex was restored to enabled. Switching to Overview and back retained the restored summary and heatmap. Settings was closed afterward.
- Native inspection also exposed unrelated provider-status labels above Activity's chart. Those labels were removed from the Activity pane while quota status remains in Usage Center and Capacity. The native-content regression failed before the repair; 92 related tests passed afterward, with all-source Ruff and diff checks clean. This last layout change is building in `/var/tmp/jrbar-activity-final-build.XZWyOa`, output `/var/tmp/jrbar-activity-final-output.hQpP3q`; it is not yet in PID 63351.

- Latest continuation: 68 focused tests passed across Screen Bar signal fidelity, native gradients, Activity worker delivery, native Overview content, and Codex lineage. Edited-file Ruff and `git diff --check` passed. Activity now retains its selected-history summary with its cached model and restores it when reopening the pane. Both summary regressions failed before the repair. These Activity changes are source-only and require a rebuilt candidate.
- Codex lineage review found nested-fork overcounting and sibling-fork undercounting. The parser now distinguishes inherited events using fork creation boundaries and the admitted ancestor graph, while keeping each branch's new events separate. Focused regressions pass. Annual index capacity, full-suite validation, and native totals remain unverified for this source.

- Provider and account rows use aliases or bounded safe labels. Privacy mode hides account names and email addresses. Quota data remains separate from transcript and API-equivalent statistics.
- Provider refreshes are generation-fenced. Disabled integrations do not read credentials. Authenticated requests reject cross-origin redirects. Future-dated sync packets cannot pin state.
- A quiet agent with one unambiguous provider-reported account at zero percent releases its power hold after two readings and a 45-second quiet window. JR-Bar does not mark the agent complete or stop its process.
- Reset events keep per-channel receipts and retry eligible suppressed channels for no more than five minutes. Notification, Screen Bar, physical light, and confetti behavior remain separately configurable.
- The compact menu, Usage Center, settings categories, lighting controls, onboarding, and heatmap consume shared projections instead of separate polling planes.
- T3 Code and optional Agent Deck snapshot compatibility are local and read-only. T3 Code is not orchestrated. Built-in Agent Deck controls independently map device keys to explicit app actions. Creator Micro 2 requires an approved device identity and yields ownership to external Agent Deck when snapshot compatibility is configured.
- Screen Bar glow layers use native Core Graphics gradients. Source and signed-candidate spot checks reduced steady CPU from the prior 19 to 23 percent loop to roughly 1 to 3 percent when static, with higher bounded work during animation and provider refresh.
- Physical SidePulse smoke programming and byte-for-byte restoration passed on the mounted device.

## Agent Deck implementation history

- Added owner-private saved mappings, app selection, shortcut recording, and a separate action-enable switch in Devices settings. App bundle IDs remain in details rather than the primary labels.
- One HID worker handles output and input. Device notifications only select saved actions. A half-second deadline and bounded main-thread handoff prevent stale input replay.
- Corrected firmware-envelope parsing and same-report input retention. Output now includes colour, brightness, and effect fields instead of only a key ID. Shared colour and dimming settings feed those fields.
- Added generation fencing for connection choices and a lifecycle lock for shutdown/reconfiguration. Input is revoked before disabling or replacing its runtime. Native key-up is attempted even if key-down posting raises.
- Latest focused command: `.venv/bin/python -m pytest tests/test_deck_*.py tests/test_creator_micro_adapter.py tests/test_optional_integration_runtime.py tests/test_architecture_ratchets.py tests/test_provider_usage_status_bar_contract.py tests/test_every_menu_action_responds.py -q`. Result: 174 passed in 12.85 seconds. Edited-file Ruff and `git diff --check` passed.
- Native settings render: `/var/tmp/jrbar-deck-ui.yAqyvD/shortcut-updated.png`. The real selector route changed the selected mapping and displayed TextEdit with its full bundle ID in details. No real shortcut was posted and no user mapping was saved during verification.
- The new Developer ID candidate is `/var/tmp/jrbar-deck-build.aZ3tXs/pyinstaller/SidePulse.app`. Strict deep signature verification passed. The prior candidate was stopped and this exact app launched as PID 41759. Its unsigned local PKG is `/var/tmp/jrbar-deck-output.T2nDoz/SidePulse-0.6.0-arm64.pkg`, SHA-256 `d45cc4332d15d743a08d0c43042c5399c744018666ac4c285ce3bdf133a82318`. It is not notarized or release-ready.
- In that running app, Quick Settings opened Settings, Devices & Screen Bar exposed the new controls, and selecting App shortcut revealed the app picker and recorder. The recorder captured Shift-Command-K. Connection and actions remained off, no mapping was saved, and no shortcut was sent to another app.
- The prior candidate's short CPU sample was 8.8 to 16 percent on a heavily loaded host. It is not a completed performance gate or a measurement of this new candidate.
- Remaining deck work: device-layer onboarding/restoration, live input/output and ownership proof on Creator Micro 2, and installer-based verification. The optional external snapshot producer is not required by built-in controls.
- Full regression command: `.venv/bin/python -m pytest tests -q`. Result: 8,889 passed, seven subtests passed, four warnings in 350.64 seconds, exit zero. This run includes the deck tranche and firmware-envelope repairs.

## Earlier verification receipts

- Activity candidate: `/var/tmp/jrbar-activity-build.cSqa1x/pyinstaller/SidePulse.app`, launched as PID 83909 after confirming PID 97185 stopped. Build and strict deep signature verification passed. Unsigned local PKG: `/var/tmp/jrbar-activity-output.nkK1Vd/SidePulse-0.6.0-arm64.pkg`, SHA-256 `e13135ea36d5aea2d77ad43dd5550568cc2394246ccb5c60d0b097daec3eeca5`. It is not notarized, installer-verified, or an updater candidate.
- Native Activity proof: `/var/tmp/jrbar-activity-ui.hJYmKt/activity-loaded.png`. The real Settings page transitioned from scanning to a chart and 365-day heatmap. Overview displays `About JR-Bar 0.6.0`. Settings was closed after the check. No preferences or device mappings were changed.
- The live chart exposed a provider-filter mismatch and inflated Codex counts. The running candidate proves loading, not correct totals. Read-only lineage review reproduced two identical 100-token rollouts reporting 200 tokens. One sampled logical session appears in 26 files; forked histories also repeat inherited cumulative usage. The counting repair is in progress. Do not reuse the displayed 603.7B Codex total as a verified usage fact.
- Full suite for the index/open-validation candidate completed: 8,947 passed, seven subtests passed, four existing multiprocessing-fork warnings in 333.33 seconds, exit zero. This predates provider-filter and lineage repairs.
- Warm Activity repair: `_physical_file_unchanged` now checks leaf metadata and read access without opening the transcript. Actual source reads still use descriptor validation. The content-free 100-file probe measured Claude opens at 1.692 seconds versus metadata checks at 0.002 seconds; Codex measured 0.960 versus 0.001 seconds. The new no-transcript-open regression failed before the repair. Replacement, symlink, directory, removal, and read-permission revocation tests pass.
- Real 365-day Activity model now completes. First pass: 3.62 seconds, three changed transcript reads totaling 1,089,009 bytes. Repeat: 2.69 seconds, zero transcript reads and bytes. Both returned identical totals, a 365-day heatmap, and a summary. This proves the source data build; native Activity rendering is the next check.
- Current source suite for the usage tranche: 146 tests passed in 10.48 seconds. Edited-file Ruff and `git diff --check` passed. A fresh app build and full test suite are running; neither is complete at this checkpoint. Creator Micro enumeration still found zero matching collections, without opening a device.
- Latest focused source check: 141 tests passed in 18.31 seconds across usage indexing, cache bounds, coverage, incremental tails, graph-worker delivery, heatmaps, and architecture. The 14 Screen Bar signal regressions also passed again. Edited-file Ruff and `git diff --check` passed.
- Activity source repairs now include a private per-file SQLite index, corruption fallback, retained history across range changes, and append-only transcript reads. A failed graph worker now shows a retry message, and a successful retry clears it. These changes are not in the running signal candidate.
- The real-history scan completed indexing but exposed a heatmap exception above 100,000 records. The heatmap now consumes the scanner's complete admitted sequence and updates session sets in place. Its 100,001-record regression first failed, then passed with exact totals. A later warm end-to-end probe remained unfinished after more than 90 seconds and was interrupted. Its sample is `/var/tmp/jrbar-activity-warm-50934.sample.txt`; usable Activity and warm performance remain unverified.
- Screen Bar signal-fidelity repair: removed pixel-level brightness floors, automatic identity collapse, and the rounded band's colored halo and highlight. The optional outline is neutral. Native gradients preserve empty edges and gaps; narrowed Alcove bands fit all eight LEDs; bracket risers follow their corresponding endpoint LEDs. The new regressions failed before each repair. The combined renderer check passed 659 tests, the legacy lighting check passed six, and architecture, packaging, lighting-settings, and Overview checks passed 42. Ruff and `git diff --check` passed.
- Signal candidate: `/var/tmp/jrbar-signal-build.MrS9iQ/pyinstaller/SidePulse.app`. Build and strict deep code-signature verification passed. Local unsigned PKG: `/var/tmp/jrbar-signal-output.grMlv9/SidePulse-0.6.0-arm64.pkg`, SHA-256 `9f999ab878f9605b9dcd2e96016143835cc42f75230a5b36993fa899eebc496b`. It includes the HID/runtime repairs and About metadata fix. PID 53660 was stopped and this candidate launched as PID 97185. No installer-based or notarization claim is made.
- Live signal proof: `/var/tmp/jrbar-signal-ui.h6ZcIM/before.png` captures the previous multicolor wash. The inspected `after-1.png` through `after-4.png` show dark intervals followed by a localized moving light. The candidate's actual Overview says `About JR-Bar 0.6.0`. No user settings or Creator Micro mappings were changed during this check.
- Activity diagnosis supersedes the earlier single-stack hypothesis: the 60-second probe read 1,590 files and 1.37 GB, spending 48.54 seconds in reads. The apparent blocked `os.open` had been running for only 0.028 seconds at the earlier deadline. The valid Claude cache contains 272 of 3,195 admitted files, leaving 2,923 files and 2.18 GB to be reread. The 8 MiB aggregate cache cannot retain a year of positive files. A persistent per-file index is the next repair; simply increasing the timeout would not resolve it.
- Settings navigation repair: Overview no longer builds the transcript chart or event-hook controls. Usage now owns an Activity tab with the existing chart and heatmap. The regression first failed on both misplaced Overview controls and the missing Activity route. The settings, chart, global action, and architecture check passed 101 tests. Four legacy settings and lazy-pane checks also passed.
- Settings candidate: `/var/tmp/jrbar-settings-build.zSk6A5/pyinstaller/SidePulse.app`, launched as PID 53660 after stopping PID 41759. Build and strict deep code-signature verification passed. Local unsigned PKG: `/var/tmp/jrbar-settings-output.lEAZca/SidePulse-0.6.0-arm64.pkg`, SHA-256 `14b3d2cd8a0a9250f851860e1cb52101d891a5ded09f5a20f0e20be255b304dc`.
- Native settings proof: `/var/tmp/jrbar-settings-ui.fLrUl5/overview.png` and `activity.png`. Overview shows global actions and About without a chart; Usage routes to Activity and displays its scanning state. This is a directly launched app, not an installer-based check.
- Full regression session 34561 finished: 8,890 passed, one failed, seven subtests passed, four warnings in 406.81 seconds. The failure was a legacy test expecting the usage label on Overview. It now explicitly builds Activity; that test and both new navigation regressions passed in 6.95 seconds. The full command has not been rerun after this test-only correction.
- Deck review repairs now implemented in source: closed optional runtimes cannot publish late settings values or UI callbacks, and disk reads no longer hold the lifecycle lock. The macOS transport applies and confirms nonexclusive mode through the pinned HID binary before opening. It refuses the open when those APIs are unavailable. Response-ID collision checks still do not guarantee exclusive device ownership; the documentation and conflict message now say so.
- Combined settings, HID, optional-runtime, deck-controller, native-action, and architecture check: 116 passed in 11.23 seconds. Four new macOS open-mode tests failed before the repair; the shutdown regression also failed before its repair. Ruff and `git diff --check` passed.
- Native HID policy probe used the pinned 0.14.0 library from the settings build environment. The policy changed from 1 to 0 through the production helper. No device was opened. This proves the C API binding, not physical compatibility.
- The running PID 53660 and settings PKG above predate the HID/runtime repairs. Do not treat that candidate or the earlier full-suite result as verification of those changes. No build or test process remains running at this checkpoint.
- Additional observed packaging issue: About displays `JR-Bar dev` in the signed candidate. Package version metadata is not available through its current lookup. Correct the bundled version display before release.
- Activity remains in its scanning state after several minutes in the settings candidate. A content-free 15-second direct data-build probe timed out inside `_read_verified_prefix` at `os.open`, called from the Claude transcript parser. The native process sample is `/var/tmp/jrbar-settings-ui.fLrUl5/activity-scan.sample.txt`. The data-loading check is incomplete; tab navigation alone does not prove usable charts. The first diagnostic probe was stopped after its catchable timeout failed to end it. No diagnostic processes remain.

- Focused renderer, integration CLI, and architecture suites: 35 passed.
- Repository-wide Ruff: passed.
- Shell syntax: passed using each script's declared interpreter.
- Previous complete source run before the native-gradient extraction: 8,798 passed, four warnings, and seven subtests passed, with one architecture size-ratchet failure.
- Post-extraction complete source run: 8,799 passed, four warnings, and seven subtests passed in 554.91 seconds.
- T3 Code live bounded probe: compatible, 13 threads, zero active, zero needing input, not truncated.
- Physical SidePulse verification: one device passed, smoke result zero, restored hash matched, and modification time changed.
- Creator Micro 2 packaged backend enumeration: zero matching devices connected. No physical compatibility claim is made.
- Developer ID app signature: `codesign --verify --deep --strict` passed on the rebuilt app.
- Gatekeeper: rejected the rebuilt app as `Unnotarized Developer ID`, as expected before notarization.

## Security audit receipts

- Latest scan: `7cea6e4e-5a6b-4466-a6ee-bfcd04f79b29`, completed on 2026-09-05 with two low findings.
- Snapshot digest: `codex-security-snapshot/v1:sha256:eee538c28cea77feef755dc3dc0e95c94b0f0b8873ab7759b8abbdc85a2e4059`.
- Report: `/private/var/folders/ph/7cfrg6mn3bj120hdsfs79d140000gn/T/codex-security-scans-kll5ew/sidepulse-JR-Fork/7d2dedfdd5d2c5fbb350cb997b43a12fbaeb0fd0_20260905T022035Z_6o1lu69q/report.md`.
- This report is immutable pre-repair evidence. Subsequent source changes do not turn it into a zero-finding scan. Fix verification is tracked separately.
- Recorded scan usage: 31,289,345 total tokens, including 31,158,691 input tokens, of which 29,427,840 were cached, and 130,654 output tokens across 13 recorded tasks.

Earlier scan receipts follow. Each applies only to its recorded snapshot.

- Scan: `73672d06-12f8-48eb-b020-953541992366`
- Target: `7d2dedfdd5d2c5fbb350cb997b43a12fbaeb0fd0`
- Result: 9 findings. Three high, two medium, and four low.
- Report: `/private/var/folders/ph/7cfrg6mn3bj120hdsfs79d140000gn/T/codex-security-scans-AEU06n/sidepulse-JR-Fork/7d2dedfdd5d2c5fbb350cb997b43a12fbaeb0fd0_20260904T170926Z_ajvu27jo/report.md`
- Remediation: regression tests cover exact browser consent, provider redirects, sync timestamps, local read authentication, diagnostics privacy, eject scope, release-runner isolation, and dependency hashes.
- The scan reviewed every mapped high-risk area. It did not fully read every tracked test, document, or asset, so the report records partial file coverage instead of claiming full repository coverage.

- Complete diff scan: `8380b173-5994-4fbf-a229-6436f27163bd`.
- Diff result: four validated findings covering privacy digest leakage, integration-disable routing, and spoofable Creator Micro identity. Each finding has a regression test and repair.
- Post-remediation scan: `b229ac29-3724-474d-a3e8-b80392d3eb8f`.
- Post-remediation result: zero findings.
- Sealed digest: `codex-security-snapshot/v1:sha256:3358ab34f8611503f819f60614b5e9820c51871486b5f3e29d7d57be38702407`.
- Post-remediation report: `/private/var/folders/ph/7cfrg6mn3bj120hdsfs79d140000gn/T/codex-security-scans-AEU06n/sidepulse-JR-Fork/7d2dedfdd5d2c5fbb350cb997b43a12fbaeb0fd0_20260904T200811Z_k8binna7/report.md`.
- Final changed-source scan: `2b81fd43-9bdf-4979-8eec-e2923f54ad58`.
- Final result: complete coverage across 98 worklist rows and zero reportable findings.
- Final sealed digest before this receipt line: `codex-security-snapshot/v1:sha256:fdd2142b0101ec8acb6d9cf81342a734062e168fccfe4a567bb6ff1c27c8ab46`.
- Final report: `/private/var/folders/ph/7cfrg6mn3bj120hdsfs79d140000gn/T/codex-security-scans-AEU06n/sidepulse-JR-Fork/7d2dedfdd5d2c5fbb350cb997b43a12fbaeb0fd0_20260904T210844Z_vp17eoav/report.md`.

## External release blockers

- Native access is restored. Privacy-toggle verification awaits approval to temporarily enable privacy mode and restore its original off state. No account name or provider permission will be changed.
- The iOS companion still needs a real iPhone or iPad HTTPS check using a trusted private CA and matching IP certificate. The full unsigned Xcode build passes. Device inventory showed a simulator only, not a connected physical iPhone. No device trust settings have been changed.
- No Developer ID Installer identity is available, so the PKG is unsigned.
- The expected notarization profile, `sidepulse-notary`, was absent at the last credential check. An available profile must be identified or configured securely before notarization and stapling.
- The Sparkle key is present under the expected `io.sidepulse.app` Keychain account and returns the pinned public key. Its final signed archive and appcast receipts still depend on a notarized candidate.
- Creator Micro 2 is detected over Bluetooth LE and physical setup, restore, and readback passed. Input and visible lighting still need a single-controller trial after the user removes the Codex device connection. If changing to USB, select USB on the device first, then remove the Codex connection because reconnection may let Codex discover it again. Do not ask for a physical key test until JR-Bar alone is ready to receive it.
- Xcode and Instruments are available through the task-specific developer directory. Controlled performance scenarios, matching runtime exports, and the full seven-scenario matrix remain incomplete. The mixed-activity diagnostic trace is not a substitute for them.
- Until these receipts exist, JR-Bar must not be merged, pushed, tagged, or published as a production release.

## Stop rule

Do not merge, push, tag, or publish until every acceptance gate passes against one exact candidate.
